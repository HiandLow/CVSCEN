# AI Agent 절대 준수 규칙 (CRITICAL RULES)

본 프로젝트(VSCEN)에서 AI 에이전트(Antigravity)가 코드를 실행하거나 커맨드라인을 조작할 때 **반드시 준수해야 하는 최우선 규칙**입니다.

### 1. 파이썬 실행 환경 (Conda Environment)
- 이 프로젝트의 모든 파이썬 스크립트 실행은 절대 깡통 파이썬(`python`)으로 돌리지 마라.
- 무조건 `conda run -n "가상환경 이름" python [파일명]` 형태로 가상환경을 명시적으로 활성화하여 실행하라.
- (예시) `conda run -n H python execute.py --dataset ihdp`

현재 사용 가능한 가상환경: "H"

### 2. 권한 승인 창(Permission Modal) 남용 금지
git 원격 저장소에 변화를 주는 명령(push 등)을 제외하고 모든 명령은 사용자의 허가를 구하지 않고 작업을 진행하라. 

### 3. 백그라운드 실험 실행
- 백그라운드 태스크로 실험을 돌려놓고 슬립(Sleep) 모드에 들어갈 때는, 반드시 작업이 **성공적으로 시작되었고 에러 없이 돌아가고 있는지 로그를 주기적으로 모니터링**하며, 에러가 발생하였다면 추가 승인 없이 알아서 고쳐서 작업을 진행할 수 있도록 하라. 또한 주기적으로 작업의 진행상황을 따로 명령하지 않아도 보고하라.
- 따로 테스트나 작업을 맡기지 않았을때 자체적으로 돌아가는지 실행하는 간단한 에러, 러닝 테스트 말고 자체적으로 벤치마크나 성능 테스트를 진행하지 마라. 이런 시간이 오래걸리는 백그라운드 실험들은 명시적인 지시, 요청이 있을 경우에만 진행하고 명시적인 지시가 없을 경우, 자체적으로 짧은 에러, 작동 테스트만 가능하다.

